namespace Ads.Domain.Constants
{
    public class SettingConstants
    {
        public const string Theme = nameof(Theme);
        public const string PageVisibility = nameof(PageVisibility);
    }
}
